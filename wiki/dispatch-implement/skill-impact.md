# dispatch-implement

## skill-impact

- 2026-09-22 | proposal: initial skill (spawn chain + orchestration protocol for design↔implement pairs) | landing: SKILL.md + scripts/spawn-implementer.sh + scripts/check-env.sh, installed global | evidence: session 01a0c742-9d9a (orca routing doc work), verb surface verified via `orchestration --help` on app 1.4.205; worktree-id resolution smoke-tested against 21 live orca worktrees | status: accepted
- 2026-09-22 | correction (user): pi expands only the FIRST /skill: per prompt — dual-prefix bootstrap was broken | landing: SKILL.md §Bootstrap prompt rewritten, channel-routed (orchestration = /skill:implement only, worker preamble carries protocol; terminal fallback = two sends, skills before task) | commit 29c5660 | status: accepted
- 2026-09-22 | review round (2 fresh-context subagents, writing+accuracy): 1 blocker (BRANCH_SLUG env-prefix crash under set -u), 4 major (result-envelope parsing, one-Run contradiction, verified-chain overstatement, stale [caller identity] cross-ref) + minors all accepted | landing: script error-guarded parsing + --run reuse flag + lowercase slug; SKILL.md channel-routed bootstrap, retry-of on dispatch_id with --spec, closure terminal-list bridge + UNVERIFIED auto-close marker, description trimmed to pointer discipline; check-env PASS/WARN/FAIL format | commit e13d43d | status: accepted
- 2026-09-22 | first live spawn (peer session 01a0c808, repo <nx-repo> ticket #34) — 4 drift findings, all absorbed into skill+script: ① worker-start rejects creation flags (--name/--setup/...) for existing worktrees → pass only --task --worktree --agent; ② run-create id = result.run.id (run_* prefix), top-level result.id is the mutation-request uuid (wrong pick caused run_not_found + 3 wasted calls); ③ worktree list fs-watch lag on freshly created worktree → script retries 10x1s; ④ worker-start success output ambiguous → first call succeeded, retry errored — new Ambiguity rule: dispatch-show --task before any retry. Plus process fix: peer had edited the INSTALLED skill copy (lost on reinstall) → drift-fix rule now in SKILL.md (source repo + npx skills add -g). Known residue: probe Run "drift-probe-delete-me" left in orca (no delete verb found) | commit pending | status: accepted
- 2026-09-22 | correction (live spawn #2, worker transcript audit): orchestration dispatch embeds spec in worker preamble — pi slash expansion bypassed entirely, /skill:implement stayed literal, worker implemented WITHOUT the implement skill discipline (straight to code+commit) | landing: bootstrap spec replaced with explicit FIRST-ACTION file-read of implement SKILL.md; coordinator verifies via worker transcript (read of SKILL.md before any edit); typed /skill: send via terminal remains cosmetic-only option; terminal-fallback path unchanged (typed sends expand) | status: accepted
- 2026-09-22 | incident (first receipt): worker_done not received by coordinator. Chain: ①probe run-create "drift-probe-delete-me" silently REBOUND the coordinator terminal to the probe Run (run-create binds creator terminal as Run home) → all check calls consumer_fenced; ②worker declared "已发送" without a toolResult — its send result never landed in transcript; receipt was actually sitting intact in the old Run inbox | recovery: orchestration run-use --id <real-run> --from <coordinator-handle> → check --terminal --run --types worker_done → --ack delivery_5a8d4cba2a89 | landing: SKILL.md Run-binding rule (no run-create from coordinator terminal mid-effort; run-use recovery path) | status: accepted
- 2026-09-22 | mechanism correction (user challenge accepted): skill non-expansion on dispatch is PREFIX-POSITION, not typed-vs-injected — /skill: expands only at position 0 of a user prompt; orca prepends worker preamble so spec can never qualify. herdr/terminal-send first-line /skill: always worked (multi-agent-collab precedent). Also: first non-expansion verdict was from a weak marker regex — re-verified via implement-skill distinctive phrases (zero hits); transcript verification method recorded in lesson | commit pending | status: accepted
