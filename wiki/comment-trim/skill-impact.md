# comment-trim — impact ledger

| date | proposal | landing | verification |
|------|----------|---------|--------------|
| 2026-09-18 | Distill one trim run (6 MRs, 7 rulings, 1 user correction) into skill rules. Accepted: (1) new redundancy class 4 **Expired facts** — constraint upheld by an external rollout (downstream CI image rebuild / another repo's release), signal = comment's subject is the rollout not a local invariant; handling = one-line pointer or MR/issue trail, stale-but-load-bearing facts get rewritten in place. (2) Class 2 canonical priority: ADR > authoritative implementation (the one others mirror) > follower implementation > MR description; review-round residue (each round re-adding rationale for the same fact) merged as time-axis duplication. (3) Keep-list addition: anti-simplification trap notes — comment defends an apparently redundant guard against an elegant-looking rewrite (real case: a review bot re-suggested the fragile `default` form across 3 rounds). (5) Workflow step 2: "is the fact still true?" asked before "is it needed?"; rewrite added as a verdict equal to deletion. | in-progress/comment-trim/SKILL.md (classes 2/4, load-bearing list, workflow step 2) | No tests/ in skill (scanner untouched, doc-only change); skill still installs via `npx skills add -g`. Evidence: 01a0b37b#1 |
| 2026-09-18 | REJECTED: helm/chart-specific note ("template comments render into deployed ConfigMaps, raise the bar, ban history info in templates"). User declined — single chart repo's concern, not a general rule; revisit only if a second chart codebase shows the same cost. | — | — |
| 2026-09-18 | Second handoff batch (trim run on a helm chart repo, YAML/shell scene) — remaining items after the first distill: (a) keep-rule **implicit cross-file coupling** (fact's readers in another file than its cause: values-map entries claimed by an extender template); (b) **reference traceability as a hard gate** — own-MR numbers and "pre-!N" history never in resident comments (→ commit message / MR description); short-form spec refs without repo path, local-process references (grilling Q numbers, tracker/wayfinder), review-finding hashes → remove (hashes: state the solved problem directly); full-path refs and external-repo MR/issue → keep. User correction anchor: "spec #39 是本地的还是仓库上的，本地引用不要推送到仓库上". (c) Assertion-comment rewrite direction = invariant + regression-guard reason, never change history. (d) Limits: YAML/shell/helm scenes have no scanner — manual ruling pass. Note: the handoff re-proposed the helm/chart template-comment rule; stands rejected per row above. | in-progress/comment-trim/SKILL.md (load-bearing list, step 2, step 4, Limits) | sanitize dir-mode CLEAN; commit d4d5f33 + this commit; reinstalled via npx skills add -g. Evidence: 01a0b37b#2 |
| 2026-09-20 | User ruling: git history is a first-class retrievable source (editors surface per-line blame; agents trace logs), so comments shed ALL history duties — authorship, dates, which-MR/commit-produced-this — not just the expired-facts and own-MR-number flavors. Principle added after the four classes: "History lives in git, not comments"; comments carry only decisions not derivable from context, docs, or git history. Generalizes class 4 signal and step-4 traceability gate into a delete-side principle. | in-progress/comment-trim/SKILL.md (principle paragraph after the four classes) | sanitize CLEAN; reinstalled via npx skills add -g. Evidence: 01a0b37b#3 |
