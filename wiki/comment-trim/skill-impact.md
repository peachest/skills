# comment-trim — impact ledger

| date | proposal | landing | verification |
|------|----------|---------|--------------|
| 2026-09-18 | Distill one trim run (6 MRs, 7 rulings, 1 user correction) into skill rules. Accepted: (1) new redundancy class 4 **Expired facts** — constraint upheld by an external rollout (downstream CI image rebuild / another repo's release), signal = comment's subject is the rollout not a local invariant; handling = one-line pointer or MR/issue trail, stale-but-load-bearing facts get rewritten in place. (2) Class 2 canonical priority: ADR > authoritative implementation (the one others mirror) > follower implementation > MR description; review-round residue (each round re-adding rationale for the same fact) merged as time-axis duplication. (3) Keep-list addition: anti-simplification trap notes — comment defends an apparently redundant guard against an elegant-looking rewrite (real case: a review bot re-suggested the fragile `default` form across 3 rounds). (5) Workflow step 2: "is the fact still true?" asked before "is it needed?"; rewrite added as a verdict equal to deletion. | in-progress/comment-trim/SKILL.md (classes 2/4, load-bearing list, workflow step 2) | No tests/ in skill (scanner untouched, doc-only change); skill still installs via `npx skills add -g`. Evidence: 01a0b37b#1 |
| 2026-09-18 | REJECTED: helm/chart-specific note ("template comments render into deployed ConfigMaps, raise the bar, ban history info in templates"). User declined — single chart repo's concern, not a general rule; revisit only if a second chart codebase shows the same cost. | — | — |
