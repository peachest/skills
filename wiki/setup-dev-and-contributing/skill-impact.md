# setup-contributing — Impact Log

One line per behavioral change: proposal / landing / commit / verification.

| Date | Proposal | Landing | Commit | Verification |
|------|----------|---------|--------|--------------|
| 2026-09-14 | New skill: scaffold a repo's docs/contributing/ doc set (contribution + development) in the setup-matt-pocock-skills template pattern (explore → ask → confirm → write). Structure from NeMo Relay contribute (4-guide split + Start-When index), sglang fork AGENTS.md→DEVELOPMENT.md dimension (fork remote model, upstream sync, change markers). | in-progress/setup-contributing/ (SKILL.md + templates/{README,development-setup,workflow-and-reviews,testing-and-docs}.md) | (this commit) | Cold start — no prior runs. SKILL.md process dry-checked against both reference repos; templates carry placeholder comments for every repo-specific fact. Not yet installed to global agent. |
| 2026-09-14 | Grilling session (design tree) reshaped the conditional model: rejected per-dimension toggle mechanism, posture presets, fragment assembly, and choice persistence — only one conditional dimension survived (fork sections), so posture (Section A) directly decides them, no second question. Rejected HAMi contribution gates (AI-disclosure, author-understanding, real-env validation, scope) entirely; rejected design-records.md page (was HAMi docs/develop/ + sglang §6 flavored) — repos that need design records already have their own convention. Doc set fixed at 4 pages. | SKILL.md reference lineage/Section B/C rewritten; design-records.md deleted; gates section removed from workflow-and-reviews.md template | (this commit) | Re-read all 5 output files post-edit: no HAMi references remain, no design-records references remain, fork gating documented as posture-only |
| 2026-09-14 | Direction change (grilling round 3): skill now scaffolds TWO artifacts — root DEVELOPMENT.md (development: branch/remote model, change markers, upstream sync, design records, versioning, domain guards — sglang §0–§8 pattern) + docs/contributing/ (contribution: NeMo 4-page split). Content boundary fixed at design time: submit-time commands/gates → contributing, write-time principles/guards → DEVELOPMENT.md; [<org>] prefix lives in DEVELOPMENT.md. Upstream sync moved from workflow-and-reviews.md to DEVELOPMENT.md §3 (was misplaced under contributing). Renamed setup-contributing → setup-dev-and-contributing. Design records reinstated for fork posture only (earlier rejection targeted the old contributing-shape artifact; in the dev spec they're coupled to markers/sync via the "sync notes" column). | SKILL.md rewritten; templates/DEVELOPMENT.md added; contributing templates re-bounded (dev-setup drops branch model → links it; workflow drops upstream sync → links prefix rules) | (this commit) | Boundary table re-checked against sglang DEVELOPMENT.md §0–§9 and NeMo contribute pages: every section mapped exactly once, no duplication between the two artifacts |
| 2026-09-14 | Empty-repo gap (user review): "create neither if no AGENTS.md/CLAUDE.md" contradicted the §9 goal (ensure agents see the docs). Now mirrors setup-matt-pocock behavior: ask the user which entry file to create when neither exists, don't pick for them, never silently skip. Confirmed no hard dependency on setup-matt-pocock — `## Agent skills` is a shared convention, not its private mechanism. | SKILL.md Step 4 third branch rewritten | (this commit) | Step 4 branches re-checked: exists-with-section / exists-without / neither-asks — all three land on an agent-visible pointer |
