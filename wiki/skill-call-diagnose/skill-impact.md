# skill-call-diagnose — impact ledger

| date | proposal | landing | verification |
|------|----------|---------|--------------|
| 2026-09-21 | Absorb SkillGLoW (arXiv 2609.02217) gating discipline via challenger-reviewed reduced package: close rule requires both consecutive absent-this-run traces to cover the pattern's 触发条件 ("没考到"≠"修好了"); new entry-format field 触发条件 (grep-able trigger pattern or explicit 人工判定, open entries required, backfilled at merge, same treatment as passCheck); `refuted <date> #<n>（原因）` token on 出现 line with absent-count reset (real-execution refutation reverts status to open); knowledge-extraction entries exempt from execution criteria; step 6 ledger rows pin skill@commit + case-id/pass-rate when an eval suite exists. Eval-gated absorption variant (V ≥ A−ε) deliberately NOT landed — stays in ~/research/skill-eval-landscape/06-gating-evidence.md until roadmap Stage 1.5 exists. Rejected alternative: two-stage absorbed-provisional state (challenger blockers 1/2/6: confirmation event is zero-increment vs the existing same-day post-fix verification + diagnose absent gate; one-name-two-meanings across 13 legacy entries; open→closed bypass). | wiki/README.md (条目格式/状态机/合并语义) + this SKILL.md step 5.5/6 + wiki/okb/patterns.md:3 hardcoded-copy→pointer | uv run pytest (if tests present); next diagnose run validates the coverage-checked absent/close flow |
