[project]
name = "project-wiki-tests"
version = "0.1.0"
description = "Fail-closed test suite for the project-wiki skill CLI (Python + Node runtimes)."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
