__pycache__/
.pi-subagents
.pi
.pytest_cache/

# Runtime configuration — per-node, never commit. Only *.example templates are tracked.
runtime.conf

# Secrets / environment files — never commit. Only *.example templates are tracked.
.env
*.env
!.env.example

# Generated skill outputs (bilibili-transcriber transcripts land in CWD)
# Anchored to repo root so skill references/ directories are not ignored
/references/

# teach skill python tooling (generated)
vendor/mattpocock/skills/productivity/teach/uv.lock
vendor/mattpocock/skills/productivity/teach/.venv/
