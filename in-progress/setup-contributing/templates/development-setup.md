# Development Setup

Everything needed before building, testing, or contributing to {project} locally.

## Prerequisites

<!-- toolchain with versions: e.g. Go 1.2x / Python 3.1x + uv / Node 2x, plus system deps (kubectl, docker/nerdctl, GPU drivers...) -->

## Source setup

<!-- clone + (fork posture: internal remote vs upstream, which remote to push to) -->

```bash
# {clone command}
```

## Branch naming

<!-- the scheme: e.g. <type>/<name> (feat/, fix/, refactor/, chore/) or upstream branch family -->

## Code style

<!-- formatter/linter + how to run it: e.g. pre-commit, ruff, golangci-lint; link to style guide if any -->

```bash
# {lint/format command}
```
