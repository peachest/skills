# Testing and Documentation

How to validate a change with the smallest test set that covers it, and what docs a change must carry.

## Choosing the smallest validation set

<!-- map change-area → required tests, e.g.:
- docs-only → docs check only
- {area A} → {unit tests}
- {area B} → {integration/E2E, needs cluster/hardware}
-->

## Commands

```bash
# {the repo's single verify entry point — one command that covers lint + test}
# {targeted test command for a single area}
# {build command}
```

## Documentation checks

<!-- when a behavior change requires doc updates, where docs live, how to check links/build -->

## Licensing

<!-- license expectations for contributions: header requirements, dependency licensing notes -->
