# Workflow and Reviews

From local commits through review to merge.

## Commit conventions

<!-- commit message format: conventional commits? sign-off/DCO? -->
<!-- fork posture: internal original changes carry the [<org>] prefix — see [DEVELOPMENT.md](../../DEVELOPMENT.md) for the marker rules; cherry-picked upstream commits stay verbatim -->

## Opening a {PR / MR}

<!-- where (GitHub/GitLab), template, expected description content, linked issue -->

## Review rules

<!-- what reviewers enforce, required approvals, merge strategy; what gets a change rejected. fork posture: rules relaxed relative to upstream community process (which slash commands / approvals don't apply internally) — but code quality and human review are never relaxed -->
